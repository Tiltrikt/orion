spring:
  datasource:
    url: jdbc:h2:mem:orion
    username: dev
    password: dev
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update