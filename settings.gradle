//file:noinspection GroovyAssignabilityCheck
pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        mavenLocal()
//        maven {
//            name "GitLab"
//            url "https://git.kpi.fei.tuke.sk/api/v4/projects/68034/packages/maven"
//            credentials(HttpHeaderCredentials) {
//                name = "Private-Token"
//                value = System.getenv("KPI_GITLAB")
//            }
//            authentication {
//                header(HttpHeaderAuthentication)
//            }
//        }
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/tiltrikt/orion")
            credentials {
                username = "tiltrikt"
                password = System.getenv("ORION_GITHUB_PACKAGES")
            }
        }
    }
}

rootProject.name = 'orion'

[
        "orion-common",
        "orion-common-event",
        "orion-common-kafka",

        "orion-service-domain",
        "orion-service-common",
        "orion-service-kafka-adapter",
        "orion-service-kafka-spring-boot-starter",

        "orion-client-domain",
        "orion-client-common",
        "orion-client-kafka-adapter",
        "orion-client-kafka-spring-boot-starter",

        "orion-example:fetch-registry-client",
        "orion-example:self-registration-client",
        "orion-example:api-gateway",
        "orion-example:orion-service",
        "orion-example:topic-configuration",

        "orion-example:eureka-fetch-registry",
        "orion-example:eureka-service",
        "orion-example:consul-fetch-registry",
        "orion-example:kafka-max-connections-test",


        "orion-test"

].forEach {
    include it
}

rootProject.children.each { project ->
    {
        setProjectBuildFileName(project)
    }
}

void setProjectBuildFileName(project) {
    project.buildFileName = "${project.name}.gradle"
    project.children.each { subproject ->
        {
            setProjectBuildFileName(subproject)
        }
    }
}