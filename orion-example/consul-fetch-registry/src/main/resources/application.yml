spring:
  application:
    name: simple-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        enabled: true
        healthCheckPath: /actuator/health
        healthCheckInterval: 10s
server:
  port: 8080

debug: true